add7 = (number) => {
    return number + 7;
}

console.log(add7(4));

multiply = (num1, num2) => {
    return num1 * num2;
}

console.log(multiply(2, 4));

school = (university) => {
    return university.toUpperCase;
}

console.log(school('pentecost university'));

lastLetter = (favoriteFood) => {
    return favoriteFood.length(-1);
} 

console.log('appiah agyei');